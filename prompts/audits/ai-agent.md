# Аудит: AI-агент Флоу

> Як користуватись: відкрий новий чат (краще — в окремому worktree) і встав
> увесь цей файл першим повідомленням. Аудит **лише читає** — нічого не
> править, не комітить і не чіпає справжні дані.
> Скіл `/frequency-audit ai-agent` робить те саме коротше.

---

Ти проводиш аудит виміру **«AI-агент Флоу»** застосунку Frequency — особистої
PWA для iPhone (плюс Mac-застосунок на Electron). Код — у `src/`, збірка —
один файл `dist/index.html`, дані — локальне сховище + хмара Supabase,
AI — через власний воркер (`worker/`).

**Мета виміру:** агент робить лише те, що людина підтвердила, чесно каже про результат, відповідає мовою інтерфейсу і не коштує більше, ніж треба.

## Правила роботи

- Спершу `CODEMAP.md` — **grep**, а не читання цілком. Далі — лише потрібні
  рядки потрібних файлів (`sed -n 'A,Bp'`).
- Усі 36 файлів `src/scripts/core/` склеюються в **один** `<script>` у
  порядку номерів: будь-яка функція верхнього рівня — глобальна.
- `dist/` не правити. Справжні дані (`npm run dev`, `npm start`,
  `tools/restore.sh`) не чіпати. Відтворення — лише на копії збірки
  в scratchpad або в прев'ю `.claude/launch.json` (порт 4173).
- **Репо публічне.** Звіт — лише в чат або в scratchpad. Не комітити
  його, не вставляти в коментарі коду, не класти в `prompts/`.

## Чекліст

- [ ] **Перш ніж читати AI-код — завантаж скіл `claude-api`.** ID моделей,
      thinking, effort, кешування — лише з документації, не з пам'яті.
- [ ] **Інструменти.** Для кожного запису `FLOW_TOOLS` (`12-ai-agent.js`) і
      гілки `aiToolIsWrite`: записуючий інструмент питає шторкою
      (`aiToolConfirm`) і показує **реальні знайдені** об'єкти (назва, дата,
      сума), а не те, що написала модель?
- [ ] **Ліміт.** Кожна зміна рахується в `AI_WRITE_LIMIT`; неоднозначний
      фрагмент дає моделі кандидатів, а не перший збіг?
- [ ] **Чесний результат.** Інструмент повертає те, що **справді записано**
      (або «людина скасувала»), а не намір. Модель не може сказати «додав»,
      якщо запису нема.
- [ ] **Слід і відкат.** Кожна дія лишає запис у сліді агента (`aiTrace*`) і
      журналі для відкату.
- [ ] **Обхідні шляхи.** Чи є інші способи змінити дані з відповіді моделі:
      маркери в тексті, авторежим, спот (`15-flow-spot.js`), пам'ять?
- [ ] **Підказки.** Системні підказки (агент, чат, спот, персона пета)
      не суперечать одна одній і коду; опис інструмента = його поведінка.
- [ ] **Мова.** Відповідь мовою UI (uk/en) в усіх шляхах.
- [ ] **Шов застосунок ↔ воркер.** Формат `system` (масив блоків, а не рядок),
      маршрути, які кличе клієнт, є у `worker/`; `stop_reason`
      (`max_tokens`, `refusal`) оброблено; блоки думання повертаються без змін.
- [ ] **Межі.** Пам'ять і історія обмежені за кількістю й довжиною; оцінено
      вартість одного ходу.

## Як відтворювати

- Два прогони: **без воркера** (фолбек) і **з воркером** у прев'ю.
- Сценарій: «додай витрату 50 на каву» → шторка з реальними полями →
  «Скасувати» → агент не повторює і не каже «додав». Потім «Підтвердити» →
  запис видно в UI, у сліді агента є крок.
- Мова: перемкни UI на EN і постав те саме питання.
- Червона команда prompt-injection — див. `audits/bezpeka.md`.

## Формат звіту

Кожна знахідка — окремим блоком:

```text
AI-N · серйозність: висока / середня / низька · статус: підтверджено / імовірно
Де: src/scripts/core/NN-файл.js:рядок   (кілька місць — списком)
Сценарій: що людина робить на телефоні → що бачить або що втрачає
Доказ: команда / grep / ≤5 рядків коду / кроки відтворення в браузері
Ескіз виправлення: 1–3 речення, без коду
Як перевірити виправлення: команда чи дія, після якої симптому нема
```

Наприкінці звіту:

1. **Зведення** — 3–5 речень простими словами, без жаргону (читає не програміст).
2. **Топ-3** за ризиком з одним реченням «чому саме ці».
3. **Що НЕ перевірено** і чому (нема телефона, нема входу в Google тощо).

## Скептична перевірка (обов'язково перед здачею)

Пройди кожну знахідку ще раз — як опонент, який хоче її спростувати:

1. **Рядок актуальний?** Відкрий його заново — код справді такий, номер не зсунувся?
2. **Нема запобіжника вище?** grep ім'я функції в усьому `src/`: обгортка,
   перехоплення `window.X = …`, прапорець, ранній `return`.
3. **Досяжно з реального UI?** Не лише з консолі, dev-режиму чи вигаданих даних.
4. **Відтворено чи прочитано?** Лише «видно з коду» → статус «імовірно».
5. **Не дубль?** Дві знахідки з одним коренем — злий в одну.

Викинь усе, що не пережило перевірку, і напиши: «перевірено N, відкинуто M».
