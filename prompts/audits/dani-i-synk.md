# Аудит: Дані й синхронізація

> Як користуватись: відкрий новий чат (краще — в окремому worktree) і встав
> увесь цей файл першим повідомленням. Аудит **лише читає** — нічого не
> править, не комітить і не чіпає справжні дані.
> Скіл `/frequency-audit dani-i-synk` робить те саме коротше.

---

Ти проводиш аудит виміру **«Дані й синхронізація»** застосунку Frequency — особистої
PWA для iPhone (плюс Mac-застосунок на Electron). Код — у `src/`, збірка —
один файл `dist/index.html`, дані — локальне сховище + хмара Supabase,
AI — через власний воркер (`worker/`).

**Мета виміру:** жоден сценарій (гість → вхід, холодний старт без мережі, два пристрої, міграція, скидання) не губить і не затирає дані людини в хмарі.

## Правила роботи

- Спершу `CODEMAP.md` — **grep**, а не читання цілком. Далі — лише потрібні
  рядки потрібних файлів (`sed -n 'A,Bp'`).
- Усі 36 файлів `src/scripts/core/` склеюються в **один** `<script>` у
  порядку номерів: будь-яка функція верхнього рівня — глобальна.
- `dist/` не правити. Справжні дані (`npm run dev`, `npm start`,
  `tools/restore.sh`) не чіпати. Відтворення — лише на копії збірки
  в scratchpad або в прев'ю `.claude/launch.json` (порт 4173).
- **Репо публічне.** Звіт — лише в чат або в scratchpad. Не комітити
  його, не вставляти в коментарі коду, не класти в `prompts/`.

## Чекліст

- [ ] **Реєстр ключів.** Кожен ключ, який читає чи пише код, є у `FLOW_KEYS`
      (`01-base.js`, `grep -n FLOW_KEYS CODEMAP.md`)? Сирі
      `localStorage.getItem('flowapp_…')` в обхід `window.storage` — є?
- [ ] **Автоматичні записи** (міграції, `ensure*`, заводські дефолти) — для
      папок лише `saveFolders({auto:true})`, для інших ключів — такий самий
      запобіжник? Перед записом перевіряється, що дані сесії прочитано
      (`foldersLoaded`, `window.sbDataTrusted()` у `02-storage.js`)?
- [ ] **«Порожньо ≠ не відповіло».** Що робить завантаження, коли хмара
      повернула `null`, помилку чи таймаут? Для кожного великого ключа
      (папки, дошки, чати, фінанси): чи може заводська заглушка перезаписати
      справжні дані в хмарі?
- [ ] **Гість → вхід у Google.** Що станеться з даними, створеними до входу,
      і з тими, що вже є в хмарі?
- [ ] **Два пристрої за 2 хвилини.** Ключ пишеться цілком? Хто перемагає —
      новіша мітка годинника? Чи губиться запис з іншого пристрою?
- [ ] **Разові міграції** (`*Once`, `migrate*`, виклики з `loadOnce()` у
      `27-canvas.js`): є прапорець «виконано»? Ставиться лише після довіреного
      читання? Ідемпотентна? **Копіює**, а не видаляє старий ключ?
- [ ] **Бекап і скидання.** Ключ потрапляє в експорт (Ще → Бекап даних)?
      Відновлення з бекапу не програє хмарі? «Скинути» справді скидає?
- [ ] **Квота.** Нема base64 (`toDataURL`, `readAsDataURL`) у ключах сховища —
      фото й аудіо лише через `photoPut` (PhotoDB, посилання `idb:`)?
- [ ] **Mac-застосунок.** Яка збірка встановлена і чи не пише вона старим
      кодом у ту саму хмару?

## Як відтворювати

- Заповни таблицю для кожного ключа, який зачіпає знахідка:

  | ключ | пишеться цілком? | хто пише автоматично | null з хмари | гість→вхід | офлайн-старт | 2 пристрої | бекап/скидання | байтів у квоті |
  |---|---|---|---|---|---|---|---|---|

- Відтворення — лише в прев'ю (порт 4173) з тестовим профілем, мережу
  вимикаєш у DevTools або підміняєш відповідь хмари в копії збірки.
- **Ніколи** на справжніх даних. Якщо без них ніяк — спершу
  `./tools/snapshot.sh` (рятує лише сховище Mac, не хмару й не телефон)
  і пряме «так» від Ярослава.

## Формат звіту

Кожна знахідка — окремим блоком:

```text
DATA-N · серйозність: висока / середня / низька · статус: підтверджено / імовірно
Де: src/scripts/core/NN-файл.js:рядок   (кілька місць — списком)
Сценарій: що людина робить на телефоні → що бачить або що втрачає
Доказ: команда / grep / ≤5 рядків коду / кроки відтворення в браузері
Ескіз виправлення: 1–3 речення, без коду
Як перевірити виправлення: команда чи дія, після якої симптому нема
```

Наприкінці звіту:

1. **Зведення** — 3–5 речень простими словами, без жаргону (читає не програміст).
2. **Топ-3** за ризиком з одним реченням «чому саме ці».
3. **Що НЕ перевірено** і чому (нема телефона, нема входу в Google тощо).

## Скептична перевірка (обов'язково перед здачею)

Пройди кожну знахідку ще раз — як опонент, який хоче її спростувати:

1. **Рядок актуальний?** Відкрий його заново — код справді такий, номер не зсунувся?
2. **Нема запобіжника вище?** grep ім'я функції в усьому `src/`: обгортка,
   перехоплення `window.X = …`, прапорець, ранній `return`.
3. **Досяжно з реального UI?** Не лише з консолі, dev-режиму чи вигаданих даних.
4. **Відтворено чи прочитано?** Лише «видно з коду» → статус «імовірно».
5. **Не дубль?** Дві знахідки з одним коренем — злий в одну.

Викинь усе, що не пережило перевірку, і напиши: «перевірено N, відкинуто M».
